using System.Collections.Generic;
using System.Linq;
using System.IO;
using System.Reflection;
using Newtonsoft.Json;
using Newtonsoft.Json.Serialization;
using System;
using System.Threading;

namespace PoeSharp.Playground
{
    public static class DatThingie
    {
        public static void GetThoseDats()
        {
            //var datFiles = new DatFileIndex(SpecificationIndex.Default);

            //Console.WriteLine("First 10 mods:");
            //int i = 0;
            //foreach (var row in datFiles["Mods.dat"])
            //{
            //    Console.WriteLine(row["Name"]);
            //    if (++i == 10) break;
            //}

        }
    }
}